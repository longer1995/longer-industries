# Longer CRM — web PWA. Copy to .env.local and fill from your Supabase project
# (Project Settings → API). These are the ANON values — safe in the browser;
# RLS scopes every row to the signed-in user.
VITE_SUPABASE_URL=https://your-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
